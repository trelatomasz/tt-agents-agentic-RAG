# Security & Public Repository Guidelines

See full specification in [AGENTS.md](file:///d:/src/tt-agents-agentic-RAG.gh.public.git/AGENTS.md).

> **CRITICAL MANDATE**:
> - This is a **PUBLIC REPOSITORY**.
> - **ZERO IDENTITY OR CREDENTIAL LEAKS**: Never commit real GCP project IDs, personal emails, tokens, keys, or passwords.
> - Always use placeholders: `your-gcp-project-id`, `your-email@example.com`.
> - Never stage `.env` or `*.tfvars`.
> - **Execution Paths**:
>   - **Windows**: `D:\src\tt-agents-agentic-RAG.gh.public.git`
>   - **WSL**: `/home/ttrela/src/tt-agents-agentic-RAG.gh.public.git` (where `gcloud` and `tofu` reside).
>   - Keep `.venv` strictly isolated between environments.
