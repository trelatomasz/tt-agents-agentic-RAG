"""Personal Retrieval-Augmented Generation platform.

Implements `docs/personal-rag-spec.md`. The generic pipeline is deliberately independent of
`gpc_rag`: the parts pilot keeps its `Part` model and `/v1/answers` contract unchanged.
"""
